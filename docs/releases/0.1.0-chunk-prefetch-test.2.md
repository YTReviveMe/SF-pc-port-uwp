# Syphon Filter PC 0.1.0 chunk prefetch test 2

## Changes

- Uses each room's authored DAT prefetch tail to warm the next world chunk's
  native-rate appearance before its retail portal activation.
- Prefetched chunks remain excluded from rendering, depth, collision and
  gameplay until the original visibility set activates them.
- Keeps the existing 0.60-second transition for genuinely cold chunks.

## Verification

- Release targets `syphon_filter` and `sf_tests` compiled successfully.
- `sf_tests` passed.
- The game executable was not launched.
