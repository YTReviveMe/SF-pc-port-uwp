# Syphon Filter PC 0.1.0 chunk spatial fade test 1

## Changes

- Replaced the whole-chunk fog tint with a soft near-to-far per-vertex reveal.
- Limited authored prefetch lead so a waiting chunk cannot become fully visible
  before its retail portal activation.
- Fully hidden terrain polygons are not submitted and therefore cannot form an
  opaque fog-coloured chunk boundary or write depth.
- Gameplay visibility, collision and active-object ownership remain unchanged.

## Verification

- Release targets `syphon_filter` and `sf_tests` compiled successfully.
- `sf_tests` passed.
- The game executable was not launched.
